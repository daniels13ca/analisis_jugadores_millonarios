"""Streamlit dashboard for Millonarios FC team/player performance.

`data.py` is the DuckDB-backed query layer (importable and testable without
Streamlit). `app.py` is the Streamlit UI; run it with:

    streamlit run src/millos_data/dashboard/app.py

or via `python -m millos_data dashboard`.
"""
